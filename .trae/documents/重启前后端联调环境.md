## 操作计划

* 后端：在 `backend` 目录重启 `uvicorn`（`main:app`），监听 `0.0.0.0:8000`，启用 `--reload`。

* 前端：在 `frontend/eval-app` 目录重启 Vite 开发服务器（`npm run dev`），默认端口 `5173`。

* 完成后提供可访问地址以便联调：

  * 后端 `http://localhost:8000/`

  * 前端 `http://localhost:5173/`

